apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-ec-web.fullname" . }}
  labels:
    {{- include "kube-ec-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-ec-web.selectorLabels" . | nindent 4 }}
